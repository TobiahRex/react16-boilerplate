import React from 'react';
import { Switch } from 'react-router-dom';

class Dashboard extends React.Component {
  render() {
    return (
      <Switch>
        <div>React Router Layout</div>
      </Switch>
    );
  }
}

export default Dashboard;
